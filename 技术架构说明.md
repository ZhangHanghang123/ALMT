# 银行经营智能分析平台 — 技术架构说明

> 版本: V2.0 | 更新: 2026-08-06

---

## 一、技术栈

| 层级 | 技术选型 |
|------|----------|
| **前端** | React 18 + TypeScript + Vite + Ant Design 5 |
| **后端** | Python 3.11 + FastAPI + SQLAlchemy 2.0 |
| **数据库** | MySQL 8.0 (主库) + Redis 7.0 (缓存) |
| **LLM** | DeepSeek V4 (ChatOpenAI兼容接口) |
| **PDF解析** | pypdf + 正则匹配 |
| **认证** | JWT + bcrypt + passlib |
| **部署** | Ubuntu 22.04 + Nginx + Systemd |
| **代码仓库** | GitHub: ZhangHanghang123/ALMD |

---

## 二、系统架构

```
浏览器 → Nginx(:80) ──→ /api/* → Uvicorn(:8000) → FastAPI
                    ──→ /*     → 静态文件 /var/www/almd/
                    
FastAPI 路由模块:
├── auth          认证（JWT登录/登出/me）
├── dashboard     首页仪表盘
├── banks         银行机构CRUD
├── indicators    指标库CRUD
├── benchmark     同业对比分析
├── chat          智能对话（LLM）
├── workflow      工作流编排
├── ontology      本体知识管理
├── report_collect 报告采集
├── liquidity     流动性压力测试
├── llm_config    LLM配置管理
├── dict          字典管理
└── indicators_dashboard 指标仪表盘
```

---

## 三、数据库设计（核心表25张）

### 系统表（sys_前缀，7张）
| 表名 | 说明 |
|------|------|
| sys_user | 用户（username/password_hash/roles） |
| sys_role | 角色 |
| sys_user_role | 用户-角色关联 |
| sys_permission | 权限 |
| sys_role_permission | 角色-权限关联 |
| sys_llm_config | LLM配置（provider/api_key/base_url） |
| sys_dict_data / sys_dict_type | 字典 |

### 业务表（ALMD_前缀，18张）
| 表名 | 说明 |
|------|------|
| almd_bank_institution | 银行机构（47家，含bank_code/bank_type） |
| almd_indicator_define | 指标定义（55+指标，含编码/公式/单位） |
| almd_indicator_value | 指标值（26,887条，含年份/数值/审核状态） |
| almd_report_record | 报告记录 |
| almd_report_file | 报告文件 |
| almd_bank_report_link | 银行-报告关联 |
| almd_collect_task | 采集任务 |
| almd_ontology_class | 本体概念（129条，82类+47实例） |
| almd_ontology_relation | 本体关系 |
| almd_ontology_version | 本体版本快照 |
| almd_indicator_mapping | 异构映射 |
| almd_workflow_def | 工作流定义 |
| almd_workflow_exec | 工作流执行 |
| almd_workflow_node_exec | 节点执行 |
| almd_chat_session | 对话会话 |
| almd_chat_message | 对话消息 |
| almd_benchmark_compare | 对比记录 |
| almd_g21_gap | G21流动性缺口 |
| almd_hqla_asset | HQLA优质资产 |
| almd_stress_version | 压力测试版本 |

---

## 四、API接口总览

| 模块 | 端点前缀 | 主要接口数 |
|------|----------|-----------|
| 认证 | /api/auth | 3 (login/me/logout) |
| 银行 | /api/banks | 5 (CRUD+列表) |
| 指标 | /api/indicators | 8 (定义+值CRUD) |
| 对比 | /api/benchmark | 5 (compare/years/save/history/upload) |
| 流动性 | /api/liquidity | 18 (G21+HQLA+版本+报告生成) |
| 对话 | /api/chat | 2 (对话+历史) |
| 采集 | /api/report-collect | 10 (采集+提取+上传) |
| 工作流 | /api/workflow | 6 (定义+执行) |
| 本体 | /api/ontology | 8 (类+关系+映射+版本) |

---

## 五、关键文件清单

### 后端核心
```
backend/app/
├── main.py              FastAPI入口+路由注册
├── config.py            配置管理（SECRET_KEY/DATABASE_URL）
├── database.py          数据库连接
├── dependencies.py      JWT认证依赖
├── routers/             13个路由模块
│   ├── auth.py          认证
│   ├── liquidity.py     流动性（最大模块，850+行）
│   ├── benchmark.py     同业对比
│   └── ...
├── services/
│   ├── report_generator.py  报告生成器（docx）
│   ├── report_collector.py  指标提取
│   └── ...
└── models/              SQLAlchemy ORM模型
```

### 前端核心
```
frontend/src/
├── App.tsx              路由+认证守卫
├── api/index.ts         API客户端（axios+拦截器）
├── layouts/MainLayout.tsx  主布局（2级菜单）
└── pages/
    ├── Login.tsx           登录页
    ├── Dashboard.tsx       仪表盘
    ├── BenchmarkCompare.tsx 同业对比（含本行对比）
    ├── LiquidityStressTest.tsx 流动性压力测试（最大页面，600+行）
    ├── ChatAnalysis.tsx    智能对话
    ├── OntologyKnowledge.tsx 本体管理
    ├── WorkflowEditor.tsx  工作流编排
    └── ...
```

---

## 六、环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| DATABASE_URL | MySQL连接 | mysql+pymysql://... |
| REDIS_URL | Redis连接 | redis://127.0.0.1:6379 |
| SECRET_KEY | JWT密钥 | (生产环境需更换) |
| ACCESS_TOKEN_EXPIRE_MINUTES | Token有效期 | 1440 |
| AUTH_REQUIRED | 是否强制认证 | true（开发可设false） |
