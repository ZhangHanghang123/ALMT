# ALMT 产品分析报告

## 一、产品概述

**ALMT** 全称为 **Asset Liability Management Tool**（资产负债管理工具），是一款面向银行及金融机构的Windows桌面应用程序。

> **注**：本次分析基于源码目录 `C:\中电金信\产品资料\ALMT\资债效率工具\源码\资产负债模拟分析工具` 深度分析后更新。该系统主要用于资产负债管理（ALM）的核心功能，包括账户层级管理、业务计划分摊、定价策略配置、风险权重计算、指标口径管理等模块。

---

## 二、技术架构分析

### 2.1 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **运行框架** | .NET Framework 4.8 | Windows桌面应用基础框架 |
| **UI框架** | SunnyUI.dll | 现代化Windows UI控件库 |
| **数据库** | SQLite | 本地轻量级关系数据库 |
| **ORM框架** | SqlSugar | 轻量级ORM框架 |
| **日志框架** | log4net | 标准化日志系统 |
| **数据处理** | NPOI | Excel文件读写 |
| **压缩库** | DotNetZip / ICSharpCode.SharpZipLib | ZIP压缩支持 |
| **加密库** | BouncyCastle.Crypto | 加密算法支持 |
| **JSON处理** | Newtonsoft.Json | JSON序列化/反序列化 |

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      应用层 (UI Layer)                       │
│              SunnyUI 桌面界面 + Windows 窗体                  │
├─────────────────────────────────────────────────────────────┤
│                    业务逻辑层 (BLL)                         │
│   ENGINE A │ ENGINE B │ ENGINE C │ ENGINE D │ 报表引擎     │
├─────────────────────────────────────────────────────────────┤
│                      数据访问层 (DAL)                       │
│                    SqlSugar ORM + SQLite                    │
├─────────────────────────────────────────────────────────────┤
│                      数据存储层                             │
│         ALMT.db (SQLite) │ Excel文件 │ 日志文件            │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 数据库表结构

根据日志分析，ALMT系统包含以下核心数据表：

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `system_config` | 系统配置 | uuid, data_date |
| `param_buckets` | 业务桶参数 | bucket_name, bucket_end_date, bucket_days, year_days |
| `coa_info` | 科目信息表 | coa_lvl, parent_coa_cd, coa_cd, coa_name, leaf_flag |
| `coa_attribute` | 科目属性表 | coa_cd, term, accrule_base, curve_name, curve_id, business_line, float_ratio, replace_type, reprice_freq |
| `current_position` | 当前持仓 | coa_lvl, balance, average, rate, current1~current72 |
| `param_rate_scenario` | 利率情景假设 | curve_name, curve_id, scenario_M1~M24 |
| `param_business_planning_string` | 业务计划 | coa_lvl, plan_balance1~24, plan_average1~24 |
| `param_risk_weight` | 风险权重 | coa_cd, weight |
| `param_stretagy_line_string` | 定价策略 | stretagy_M1~M24 |
| `report_rule_info` | 报表规则 | column1~column138 |
| `Log` | 系统日志 | LogId, Date, Level, Logger, Message, StackTrace |

### 2.4 配置文件结构

`ALMT.exe.config` 主要配置项：

- **日志配置**：支持RollingFileAppender（文件日志）、ColoredConsoleAppender（控制台日志）、AdoNetAppender（数据库日志）三种输出方式
- **日志级别**：默认INFO级别
- **日志保留**：最多保留10天日志文件
- **数据库日志**：存储到SQLite数据库的Log表

---

## 三、功能架构分析

### 3.1 核心引擎模块

ALMT系统包含四个独立的计算引擎，分别处理不同的业务逻辑：

#### ENGINE A - 业务计划分摊引擎

**功能**：将业务计划数据按账户册层级进行分摊计算

**输入表**：
- 接收表-账户册层级
- 接收表-存量数据情况表
- 接收表-业务计划表

**输出表**：
- 业务计划分摊-余额
- 业务计划分摊-日均
- 输出表-分摊余额
- 输出表-分摊日均

#### ENGINE B - 定价策略引擎

**功能**：根据利率情景假设和定价策略，计算各账户册的利率和收益

**输入表**：
- 接收表-账户册层级
- 接收表-底层账户册属性
- 接收表-利率情景假设
- 接收表-定价策略

**输出表**：
- 定价策略分摊
- 输出表-定价策略分摊结果
- 输出表-计财定价策略调整参考表

#### ENGINE C - 风险权重引擎

**功能**：计算各账户册的风险权重，用于风险加权资产计算

**输入表**：
- 接收表-账户册层级
- 接收表-底层账户册及属性配置
- 接收表-业务存续期
- 接收表-风险权重

**输出表**：
- 业务计划分摊余额结果
- 业务计划分摊日均结果
- 定价策略分摊结果
- 底层账户册定价策略

#### ENGINE D - 基础指标汇总引擎

**功能**：汇总计算各类基础指标，包括利息净收入、生息资产等

**输入表**：
- 接收表-账户册层级
- 接收表-底层账户册及属性配置
- 接收表-业务存续期
- 接收表-指标口径配置
- 接收表-指标补录表

**输出表**：
- 基础指标汇总
- 输出表-策略看板（累计）
- 输出表-全行指标看板
- 输出表-业务条线指标看板
- 输出表-利息净收入测算表
- 输出表-资产负债预测表
- 输出表-资负价值管理分析表

### 3.2 功能模块清单

| 模块 | 功能描述 |
|------|----------|
| **账户册管理** | 账户册层级结构维护，支持多级账户册体系 |
| **存量数据管理** | 管理各账户册的余额、日均、利率等存量数据 |
| **业务计划** | 配置未来24个月的业务计划（余额、日均） |
| **利率情景** | 配置利率曲线和多种利率情景假设 |
| **定价策略** | 配置各账户册的定价策略和利率规则 |
| **风险权重** | 配置各资产的风险权重 |
| **指标口径** | 定义利息净收入、生息资产等指标的计算口径 |
| **指标补录** | 手动补录无法自动计算的指标数据 |
| **报表输出** | 生成各类管理报表和分析图表 |

### 3.3 数据流转图

```
                    ┌─────────────────┐
                    │   Excel模板     │
                    │  (接收表数据)   │
                    └────────┬────────┘
                             │ 导入
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                      数据接收层                             │
│   账户册层级 │ 存量数据 │ 业务计划 │ 利率情景 │ 定价策略    │
└──────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                     引擎计算层                               │
│   ENGINE A  │  ENGINE B  │  ENGINE C  │  ENGINE D          │
│  (业务分摊) │  (定价策略) │ (风险权重) │  (指标汇总)        │
└──────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                      数据输出层                               │
│        策略看板 │ 指标看板 │ 测算表 │ 预测表                │
└──────────────────────────────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   Excel报告     │
                    └─────────────────┘
```

---

## 四、产品设计分析

### 4.1 产品定位

ALMT是一款面向**中小银行**的**轻量级资产负债管理工具**，定位于：

- **替代Excel手工操作**：将原来用Excel进行的ALM计算自动化
- **本地部署**：数据存储在本地SQLite，无需服务器
- **易于使用**：友好的Windows桌面界面
- **灵活配置**：支持多维度业务参数配置

### 4.2 设计特点

#### 优点

1. **轻量级架构**：基于SQLite本地数据库，部署简单，无需专业DBA
2. **模块化设计**：四个独立引擎便于维护和扩展
3. **Excel集成**：使用Excel作为数据导入导出媒介，用户学习成本低
4. **可视化界面**：SunnyUI提供现代化Windows UI体验
5. **日志完善**：多级别日志记录，便于问题排查

#### 可改进点

1. **缺乏权限管理**：当前为单用户系统，无多用户权限控制
2. **无版本管理**：缺乏数据版本控制和审计功能
3. **报表能力有限**：主要依赖Excel输出，缺乏内置报表设计器
4. **扩展性受限**：本地SQLite在数据量大时性能可能受限

### 4.3 核心业务概念

| 概念 | 说明 |
|------|------|
| **账户册 (COA)** | 银行科目的层级结构，如"现金及存放央行"→"现金_1D" |
| **存量数据** | 各账户册的当前余额、平均余额、利率等 |
| **业务桶 (Bucket)** | 按时间周期划分的数据桶，支持日/月/季/年 |
| **利率曲线** | 不同期限的利率基准曲线 |
| **重定价** | 资产或负债重新确定利率的日期和频率 |
| **风险权重** | 用于计算风险加权资产的权重系数 |
| **利息净收入** | 利息收入减去利息支出的净额 |
| **日均余额** | 统计期内的日均余额 |

---

## 五、技术实现细节

### 5.1 依赖库说明

| 库名 | 版本 | 用途 |
|------|------|------|
| SunnyUI.dll | - | UI控件库 |
| SqlSugar.dll | - | ORM框架 |
| System.Data.SQLite.dll | 1.0.114.0 | SQLite驱动 |
| Newtonsoft.Json.dll | 13.0.0.0 | JSON处理 |
| NPOI.dll | - | Excel处理 |
| log4net.dll | - | 日志框架 |
| BouncyCastle.Crypto.dll | - | 加密支持 |
| DotNetZip.dll | - | ZIP压缩 |
| ICSharpCode.SharpZipLib.dll | 1.3.1.9 | 压缩支持 |

### 5.2 数据文件清单

```
ALMT/
├── ALMT.exe              # 主程序
├── ALMT.exe.config       # 配置文件
├── ALMT.db              # SQLite数据库
├── ALMT.pdb             # 调试符号
├── *.dll                # 依赖库
├── UI/                  # UI资源
│   ├── ALMT.ico         # 应用图标
│   └── PingFangSC-Thin.ttf  # 字体
├── x64/                 # 64位原生库
│   └── SQLite.Interop.dll
├── x86/                 # 32位原生库
│   └── SQLite.Interop.dll
├── ALMT.DATA.xlsx       # 数据字典
├── ALM.ENGINEA.xlsm     # 引擎A模板
├── ALM.ENGINEB.xlsm     # 引擎B模板
├── ALM.ENGINEC.xlsm     # 引擎C模板
├── ALM.ENGINED.xlsm     # 引擎D模板
└── 0日志测试*.log       # 日志文件
```

---

## 六、总结与建议

### 6.1 产品总结

ALMT是一款功能完整的银行资产负债管理桌面工具，涵盖了账户册管理、业务计划分摊、定价策略、风险权重、指标汇总等核心ALM功能。技术选型合理，架构清晰，适合中小银行使用。

### 6.2 后续演进建议

1. **云化升级**：考虑将数据存储迁移到MySQL/PostgreSQL，支持多用户并发
2. **权限管理**：增加用户角色和权限控制功能
3. **报表增强**：内置报表设计器，支持自定义报表
4. **数据接口**：增加与核心系统、ERP系统的数据对接能力
5. **性能优化**：针对大数据量场景进行性能优化

---

*报告生成时间：2026年8月*
